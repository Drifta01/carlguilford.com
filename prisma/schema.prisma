// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = "mysql://0t9yq5mfht0rwyo7pycn:pscale_pw_RpBGdP3wJl8VEuSMb2iLiQQnGmphRSKzSclKiVtQ3H4@gcp.connect.psdb.cloud/mydatabase?sslaccept=strict"
  relationMode = "prisma"
}

model Item {
  itemId     Int      @id @default(autoincrement())
  title      String   @db.VarChar(255)
  createdAt  DateTime @default(now()) @db.Timestamp(6)
  name       String
  code       String?
  price      Float?
  suppiler   String?
  categoryId Int?

  category Category[]
  supplier Supplier[]
}

model Category {
  categoryId Int    @id @default(autoincrement())
  name       String
  items      Item[]

  @@index([categoryId])
}

model Supplier {
  supplierId Int    @id @default(autoincrement())
  name       String
  items      Item[]

  @@index([supplierId])
}
