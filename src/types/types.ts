export type Supplier = {
  id: number;
  name: string;
  address: string;
  phone: string;
  email: string;
};

export type Item = {
  id: number;
  name: string;
  address: string;
  phone: string;
  email: string;
};

export type Category = {
  id: number;
  name: string;
  address: string;
  phone: string;
  email: string;
};
